info:
  name: Получить состояние инстанса
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/waInstance{{idInstance}}/getStateInstance/{{apiTokenInstance}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: getStateInstance
    request:
      url: "{{apiUrl}}/waInstance{{idInstance}}/getStateInstance/{{apiTokenInstance}}"
      method: VIEW
    response:
      status: 200
      statusText: OK
      headers:
        - name: Server
          value: nginx
        - name: Date
          value: Fri, 17 Apr 2020 14:31:40 GMT
        - name: Content-Type
          value: application/json
        - name: Content-Length
          value: "35"
        - name: Connection
          value: keep-alive
      body:
        type: json
        data: |-
          {
              "stateInstance": "authorized"
          }

docs: Документация [GetStateInstance](https://green-api.com/v3/docs/api/account/GetStateInstance/)
