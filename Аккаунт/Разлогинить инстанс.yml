info:
  name: Разлогинить инстанс
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/waInstance{{idInstance}}/logout/{{apiTokenInstance}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: logout
    request:
      url: "{{apiUrl}}/waInstance{{idInstance}}/logout/{{apiTokenInstance}}"
      method: VIEW
    response:
      status: 200
      statusText: OK
      headers:
        - name: Server
          value: nginx
        - name: Date
          value: Fri, 17 Apr 2020 14:24:29 GMT
        - name: Content-Type
          value: application/json
        - name: Content-Length
          value: "22"
        - name: Connection
          value: keep-alive
      body:
        type: json
        data: |-
          {
              "isLogout": true
          }

docs: Документация [Logout](https://green-api.com/v3/docs/api/account/Logout/)
