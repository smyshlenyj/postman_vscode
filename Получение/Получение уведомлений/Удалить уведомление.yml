info:
  name: Удалить уведомление
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{apiUrl}}/waInstance{{idInstance}}/deleteNotification/{{apiTokenInstance}}/{{receiptId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: deleteNotification
    request:
      url: "{{apiUrl}}/v3/waInstance{{idInstance}}/deleteNorification/{{apiTokenInstance}}/{{receiptId}}"
      method: VIEW
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
              "result": true,
              "reason": ""
          }

docs: Документация [DeleteNotification](https://green-api.com/v3/docs/api/receiving/technology-http-api/DeleteNotification/)
