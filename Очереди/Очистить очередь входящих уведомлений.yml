info:
  name: Очистить очередь входящих уведомлений
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiUrl}}/waInstance{{idInstance}}/clearWebhooksQueue/{{apiTokenInstance}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: clearWebhooksQueue
    request:
      url: "{{apiUrl}}/waInstance{{idInstance}}/clearWebhooksQueue/{{apiTokenInstance}}"
      method: VIEW
    response:
      status: 200
      statusText: OK
      headers:
        - name: Server
          value: nginx
        - name: Date
          value: Mon, 20 Apr 2020 19:50:48 GMT
        - name: Content-Type
          value: application/json
        - name: Content-Length
          value: "321"
        - name: Connection
          value: keep-alive
      body:
        type: json
        data: |-
          {
              "isCleared": true,
              "reason": ""
          }

docs: Документация [ClearWebhooksQueue](https://green-api.com/v3/docs/api/queues/ClearWebhooksQueue/)
