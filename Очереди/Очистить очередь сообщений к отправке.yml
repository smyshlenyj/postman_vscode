info:
  name: Очистить очередь сообщений к отправке
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/waInstance{{idInstance}}/clearMessagesQueue/{{apiTokenInstance}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: clearMessagesQueue
    request:
      url: "{{apiUrl}}/waInstance{{idInstance}}/clearMessagesQueue/{{apiTokenInstance}}"
      method: VIEW
    response:
      status: 200
      statusText: OK
      headers:
        - name: Server
          value: nginx
        - name: Date
          value: Mon, 20 Apr 2020 19:53:58 GMT
        - name: Content-Type
          value: application/json
        - name: Content-Length
          value: "23"
        - name: Connection
          value: keep-alive
      body:
        type: json
        data: |-
          {
              "isCleared": true
          }

docs: Документация [ClearMessagesQueue](https://green-api.com/v3/docs/api/queues/ClearMessagesQueue/)
